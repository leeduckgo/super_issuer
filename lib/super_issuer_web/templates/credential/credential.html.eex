<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/credential.css") %>"/>
  <title>获奖证书！</title>
  <script src="https://cdn.jsdelivr.net/npm/vue"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.18.0/axios.min.js"></script>
</head>
<body>
  <div id="demo">
    <div class="container" v-if="credentialData">
        <img src="<%= Routes.static_path(@conn, "/images/background.png") %>" alt="" style="width: 100%;height: 100%;">
        <div class="ceri-title"><%= @credential.claim.title %></div>
        <div class="ceri-into">获奖者：</div>
        <div class="ceri-name"><%= @credential.claim.name %></div>

        <%# <div class="slogan">Slogan:</div>
        <div class="weid">weid:</div>
        <div class="slogan-text">{{claim.receiver_slogan}}</div>
        <div class="weid-text">{{claim.receiver_weid}}</div>%>
        <span class="info-text">在「<%= @credential.claim.game_name %>」中，<b><span class="price">获得<%= @credential.claim.price %><span></b></span>
        <div class="encourage">恭喜！</div>
        <div class="signature">签名</div>
        <div class="creator">发证者</div>
        <div class="signature-text"><%= @credential.proof.signature %></div>
        <div class="creator-text"><%= @credential.proof.creator %></div>
       <%# <div class="time">{{transformTime('YYYY-mm-dd HH:MM', credentialData.issuanceDate)}} -
        {{transformTime('YYYY-mm-dd HH:MM', credentialData.expirationDate)}}</div>
      </div>%>
    </div>
</body>
</html>
